import * as vscode from "vscode";
import type { StudioDeps } from "../extension.js";

// Stub — the Runs-explorer agent will replace this body.
export function registerRunsExplorer(
  context: vscode.ExtensionContext,
  deps: StudioDeps
): void {
  // intentionally empty
  void context;
  void deps;
}
