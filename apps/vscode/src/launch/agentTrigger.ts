import * as vscode from "vscode";
import type { StudioDeps } from "../extension.js";

// Stub — the agent-trigger agent will replace this body.
export function registerAgentTrigger(
  context: vscode.ExtensionContext,
  deps: StudioDeps
): void {
  // intentionally empty
  void context;
  void deps;
}
