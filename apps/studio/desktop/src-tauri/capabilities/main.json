{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Capabilities for the main window: core defaults plus app invoke commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
