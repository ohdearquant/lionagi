{
  "path": "invoke start",
  "schema": {
    "additionalProperties": false,
    "properties": {
      "metadata": {
        "description": "Skill-specific JSON to attach (e.g. show plan, review rounds). Stored verbatim; rendered as-is on the invocation detail page.",
        "type": "string",
        "x-flag": "--metadata"
      },
      "plugin": {
        "description": "Marketplace plugin packaging the skill (optional).",
        "type": "string",
        "x-flag": "--plugin"
      },
      "prompt": {
        "description": "The user's input that triggered the skill (free text).",
        "type": "string",
        "x-flag": "--prompt"
      },
      "skill": {
        "description": "Skill name: 'show', 'codex-pr-review', 'reprompt', etc.",
        "type": "string",
        "x-flag": "--skill"
      }
    },
    "required": [
      "skill"
    ],
    "title": "invoke start",
    "type": "object"
  },
  "stage": "static"
}
