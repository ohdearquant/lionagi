{
  "path": "schedule disable",
  "schema": {
    "additionalProperties": false,
    "properties": {
      "id": {
        "description": "Id of the schedule, as returned by `li schedule list` in its `id` field.",
        "type": "string",
        "x-positional": true
      },
      "reason": {
        "description": "Why this schedule is being disabled (stored in its lifecycle audit).",
        "type": "string",
        "x-flag": "--reason"
      }
    },
    "required": [
      "id",
      "reason"
    ],
    "title": "schedule disable",
    "type": "object",
    "x-positional-order": [
      "id"
    ]
  },
  "stage": "static"
}
